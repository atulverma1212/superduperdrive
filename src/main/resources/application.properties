spring.application.name=cloudstorage
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/cloudstorage5?createDatabaseIfNotExist=TRUE
spring.datasource.username=atul
spring.datasource.password=1212
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.jpa.show-sql=true

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

# jwt properties
jwt.token.expiry.ms=86400000
jwt.token.secret=@TuL:vErM@